# ===================================
# EcoProvider Production Environment
# ===================================
# Copy file ini ke .env dan sesuaikan dengan server Anda

# Application
APP_NAME="EcoProvider"
APP_ENV=production
APP_KEY=base64:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
APP_DEBUG=false
APP_URL=https://bsdgs.fun

# Database
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=ecoprovider
DB_USERNAME=root
DB_PASSWORD=your_password_here

# Cache
CACHE_DRIVER=file
SESSION_DRIVER=cookie

# Queue
QUEUE_CONNECTION=sync

# CORS Configuration
# Whitelist domain yang diizinkan mengakses API
CORS_ALLOWED_ORIGINS=http://localhost:8000,https://bsdgs.fun,https://www.bsdgs.fun

# Mail (jika diperlukan)
MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=465
MAIL_USERNAME=your_email@example.com
MAIL_PASSWORD=your_password_here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@bsdgs.fun
MAIL_FROM_NAME="EcoProvider API"

# Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# AWS (jika menggunakan S3 untuk storage)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=

# Sentry (untuk error tracking, optional)
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# ===================================
# IMPORTANT NOTES
# ===================================
# 1. Jangan commit .env ke git
# 2. Change APP_KEY dengan: php artisan key:generate
# 3. Update DB credentials sesuai server
# 4. Set APP_DEBUG=false untuk production
# 5. Pastikan file permissions correct
# 6. Run: php artisan config:cache setelah perubahan
